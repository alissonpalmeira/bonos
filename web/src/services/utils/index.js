import { formatAmount } from './formatAmount';
import { isInstalled, platform } from './isInstalled';
import { slugify } from './slugify';
import reportWebVitals from './reportWebVitals';

export {
    formatAmount,
    isInstalled,
    platform,
    reportWebVitals,
    slugify,
}
